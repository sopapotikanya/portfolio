close all
load FLIR1512.mat

I = FLIR1512./(max(max(FLIR1512)));

Imax=max(max(I));
Imin=min(min(I));
range = Imax-Imin;

mmin=ones(size(I))*Imin;
Inew = I-mmin;

Inew2=Inew/range;



subplot(2,2,1); imshow(Inew2);
subplot(2,2,2); imhist(Inew2);

% Apply the colormap
% figure 2, imshow(Inew2);
% cmap = jet(100);
% colormap(cmap);
% freezeColors

th = 0.3;

range = Imax-th;

TH=ones(size(I))*th;
Inew3 = Inew2-TH;

Inew4=Inew3/range;



figure, imshow(Inew4);

cmap = jet(100);
% Apply the colormap
colormap(cmap);
colorbar;
figure, imshow(Inew4);
figure, imhist(Inew4);

